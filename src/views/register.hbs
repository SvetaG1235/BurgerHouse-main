<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Регистрация</title>
    <style>
        :root {
            --main-color: rgb(28, 27, 23);
            --white: rgb(254, 254, 252);
            --yellow: rgb(255, 205, 10);
            --price-color: rgb(205, 180, 130);
        }

        html, body {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            width: 100%;
            height: 100%;
        }

        .main {
            display: grid;
            width: 100%;
            height: 100vh;
            background-color: black; 
            justify-items: center; 
            align-items: center; 
        }

        .forms {
            text-align: center; 
        }

        .form-group {
            margin-bottom: 20px; 
        }

        .form-control {
            height: 80px;
            width: 500px;
            background-color: var(--yellow);
            color: var(--white);
            font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
            font-size: 25px;
            border: none;
            border-radius: 5px; 
            padding: 0 10px; 
        }

        .register-form-control {
            height: 80px;
            width: 500px;
            background-color: var(--yellow);
            color: var(--white);
            font-family: 'Trebuchet MS', 'Lucida Sans Unicode', 'Lucida Grande', 'Lucida Sans', Arial, sans-serif;
            font-size: 25px;
            border: none; 
            border-radius: 5px;
            padding: 0 10px; 
        }

        button {
            cursor: pointer; 
        }

        a {
            color: var(--yellow); 
        }
    </style>
</head>
<body>
    <div class="main">
        <div class="forms">
            <h2>Регистрация</h2>
            <form id="registerForm">
                <div class="form-group">
                    <input type="text" class="register-form-control" name="fio" placeholder="Введите ФИО пользователя" required>
                </div>
                <div class="form-group">
                    <input type="tel" class="register-form-control" name="phone" placeholder="Введите номер телефона" required>
                </div>
                <div class="form-group">
                    <input type="email" class="register-form-control" name="email" placeholder="Введите ваш email" required>
                </div>
                <div class="form-group">
                    <input type="text" class="register-form-control" name="username" placeholder="Введите имя пользователя (логин)" required>
                </div>
                <div class="form-group">
                    <input type="password" class="register-form-control" name="password" placeholder="Введите пароль" required>
                </div>
                <div class="form-group">
                    <input type="password" class="register-form-control" name="confirmPassword" placeholder="Подтвердите пароль" required>
                </div>
                <button type="submit" class="register-form-control">Зарегистрироваться</button>
            </form>
            <p>Уже есть аккаунт? <a href="enter">Войти</a></p>
        </div>
    </div>
</body>
</html>
