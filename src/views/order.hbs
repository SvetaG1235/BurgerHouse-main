<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/order_style.css">
    <title>Оформление заказа | БурGер house</title>
</head>
<body>
    <div class="main">
        
        <div class="maintext">
            <h1>Оформление заказа</h1>
        </div>

        <div class="order-container">
            <div class="order-items">
                {{#each cartItems}}
                    <div class="order-item">
                        <span>{{this.name}} × {{this.quantity}}</span>
                        <span>{{this.total}} ₽</span>
                    </div>
                {{/each}}
            </div>
            
            <div class="order-total">
                Итого: {{totalPrice}} ₽
            </div>
            
           <form action="/order/create" method="POST" id="orderForm">
                <div class="form-group">
                    <label for="address">Адрес доставки:</label>
                    <input type="text" id="address" name="address" 
                        value="{{user.address}}" required>
                </div>
                
                <div class="form-group">
                    <label for="phone">Телефон:</label>
                    <input type="tel" id="phone" name="phone" 
                        value="{{user.phone}}" required>
                </div>
                
                <div class="form-group">
                    <label>Способ оплаты:</label>
                    <div class="payment-methods">
                        <label class="payment-method">
                            <input type="radio" name="paymentMethod" value="card" checked required>
                            <span>Картой</span>
                        </label>
                        <label class="payment-method">
                            <input type="radio" name="paymentMethod" value="cash" required>
                            <span>Наличными</span>
                        </label>
                    </div>
                </div>

                <button type="submit" class="submit-btn">Подтвердить заказ</button>
            </form>

        </div>
    </div>

    <script src="/javascripts/order.js"></script>
</body>
</html>